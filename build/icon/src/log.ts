import { BuildUtilsLog } from '../../utils'

export const IconLog = (title: string, data: string) => {
  BuildUtilsLog(
    `
/**
 * ==============================================
 * ===============   ${title}  ==================
 * ==============================================
 * ${data}
 * ==============================================
 */
`
  )
}

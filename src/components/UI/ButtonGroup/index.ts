/**
 * @description This component is used for FORM enhancement, so this is not in auto import scope.
 */
import WButtonGroup from './src/index.vue'

export * from './src/types'

export default WButtonGroup

import { iconCollections } from './collections'

import { genIconLists } from './index'

/**
 * @description Generate icon list with prefix. Used for icon picker
 */
export default genIconLists(iconCollections)

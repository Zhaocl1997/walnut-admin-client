import { WFormSchemaItem, WFormSchemaItemType } from './../types'

/**
 * @description Single or multiple mock function based on schema item.
 *              Fit for `select`/`checkbox`/`tag`/`tree`/`selectTree`
 */
export const pickRandom = (
  item: WFormSchemaItem,
  TYPE: WFormSchemaItemType
) => {
  const {} = item
}
